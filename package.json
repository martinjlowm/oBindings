{
  "name": "oBindings",
  "description": "Binding management for mad people",
  "scripts": {
    "build": "tstl -p tsconfig.json",
    "preparePublish": "mkdir -p artifacts/dist && sed -E \"s\/^(## Version: )(.*)$\/\\1$CIRCLE_TAG\/\" oBindings.toc > artifacts/oBindings.toc && cp -R dist/ artifacts/dist/",
    "test": "pnpm run build && echo 'Not implemented yet'",
    "watch": "tstl -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@types/wow-api": "github:martinjlowm/types-wow-api",
    "lua-types": "^2.5.2",
    "tstl": "^2.2.0",
    "typescript": "^3.5.2",
    "typescript-to-lua": "^0.26.0"
  }
}
